---
import { Image } from "astro:assets";
import evalPsic from "../assets/evalpsic.jpg";
import psicIndi from "../assets/psicindi.jpg";
import psicFami from "../assets/psicfami.jpg";
import orientacion from "../assets/orientacion.jpg";

const cards = [
  {
    title: "Evaluación Psicológica",
    imageSrc: evalPsic,
  },
  {
    title: "Psicoterapia Individual",
    imageSrc: psicIndi,
  },
  {
    title: "Psicoterapia Familiar",
    imageSrc: psicFami,
  },
  {
    title: "Orientación Familiar",
    imageSrc: orientacion,
  },
];
---

<div class="w-full max-w-7xl mx-auto px-4 mb-16">
  <h1 class="text-3xl sm:text-4xl font-semibold mb-10 text-center lg:text-left">
    Intervención Especializada
  </h1>

  <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    {
      cards.map((card) => (
        <li class="link-card">
          <div class="card-container relative border-2 border-cyan-400 rounded-3xl overflow-hidden shadow-lg hover:shadow-xl transition-shadow duration-300">
            <div class="image-container relative">
              <Image
                src={card.imageSrc}
                alt={card.title}
                quality="high"
                loading="lazy"
                class="w-full h-56 object-cover"
              />
              <div class="overlay absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center">
                <h2 class="text-white text-center text-lg sm:text-xl font-semibold">
                  {card.title}
                </h2>
              </div>
            </div>
          </div>
        </li>
      ))
    }
  </ul>
</div>
